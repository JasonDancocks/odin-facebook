	<% if current_user.liked_post?(post) %>
	<%= link_to "Remove Like", like_path(user_id: current_user.id, post_id: post.id), method: :delete %>
	<% else %>

	 <%= link_to "Like Post", likes_path(post_id: post), method: :post %>
	 <% end %>
	 <br>